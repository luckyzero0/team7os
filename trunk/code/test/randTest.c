/*randTest.c
*	Produces a few random numbers.
*	Should always produce the exact same numbers for a given -rs value
*/
#include "utils.h"

int main(){

	printf("Testing a few rand ints [%d] [%d] [%d]\n",Rand(),Rand(),Rand(), "","");

	Exit(0);
}
